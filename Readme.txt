Sprint DevToold
[A[C[C[C[C[C[C[C[C[C[C[C--go to pom.xml and <dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-devtools</artifactId>
		</dependency>
now we not need to restart the server if we do any changes in file
Note: if we do changes in pom.xml in dependency then we must restart the server to see the reflect